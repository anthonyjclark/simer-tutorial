[project]
name = "2026-icra"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "lwmr-experiments",
    "plotly>=6.7.0",
]

[tool.uv.sources]
lwmr-experiments = { path = "../lwmr", editable = true }
